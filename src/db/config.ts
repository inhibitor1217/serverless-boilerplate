/**
 * Define table names.
 */

// export const EXAMPLE_TABLE: string = "examples";

/**
 * Define table schemas.
 */

export const schemas: { [table_name: string]: string } = {
  // [EXAMPLE_TABLE]: `
  //   CREATE TABLE IF NOT EXISTS ${EXAMPLE_TABLE} (
  //     id CHAR(36) NOT NULL,
  //     content VARCHAR(255) NOT NULL,
  //     PRIMARY KEY (id)
  //   );
  // `,
};
